# 视觉模式与边界

- `existing_only`：只使用真实图片、裁切、留白、色块和确定性文字卡。
- `ai_assist`：AI 只补场景、背景和辅助视觉，真实产品由原图叠加。
- 用户已给出默认付费授权或“直接出 HTML”指令时，图片规划和 Prompt 不展示、不复审，整批直接并发发送给已配置渠道。
- 用户尚未授权付费生图时，只确认一次整批付费请求，不逐条确认 Prompt。
- 图片能力不可用时，允许回退到真实图片与信息卡，不影响文案交付。
