interface:
  display_name: "小红书多模型并发生图"
  short_description: "Produce Executor 并发生图并记录请求历史"
  default_prompt: "按 $xhs-approved-image-generator 的程序规范直接运行 Python Produce Executor；不创建 Worker，不调用内容模型。只读取 visual.json、approval.json、默认渠道配置和声明的参考图，校验当前 Prompt 哈希后并发提交全部页面，每页仅一次初始请求，只输出 generation.json 和图片。"
policy:
  allow_implicit_invocation: false
