interface:
  display_name: "小红书视觉规划"
  short_description: "Compose Worker 生成 Prompt 与参考图映射"
  default_prompt: "在同一次 Compose Worker 调用中使用 $xhs-visual-planner 输出 visual.json：共享 style_anchor、每页完整 Prompt、信息任务、真实参考图映射和差异化构图；输出后由主控展示 Prompt 包并等待哈希批准。"
policy:
  allow_implicit_invocation: false
