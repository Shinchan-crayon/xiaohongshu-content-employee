interface:
  display_name: "小红书自然化审校"
  short_description: "一次改写标题、正文与轮播的机械表达"
  default_prompt: "使用 $xhs-humanize-review 只改写现有 content.json 的标题、正文和轮播可见文字，降低 AI 痕迹并保留既有格式、JSON 结构、追溯字段和视觉 Prompt。"
policy:
  allow_implicit_invocation: false
