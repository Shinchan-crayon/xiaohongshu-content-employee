interface:
  display_name: "小红书 HTML 交付"
  short_description: "生成 HTML 后立即交付，再做一次轻量收尾"
  default_prompt: "使用 $xhs-html-delivery 生成独立 HTML；文件可用后立即先把绝对路径交给用户，再做最多一次轻量收尾检查。"
policy:
  allow_implicit_invocation: false
