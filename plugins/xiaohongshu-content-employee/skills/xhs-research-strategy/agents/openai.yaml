interface:
  display_name: "小红书调研策略"
  short_description: "Research Worker 建立证据与选题追溯"
  default_prompt: "在独立无历史 Research Worker 的唯一一次模型调用中，同时使用 $product-material-intake 和 $xhs-research-strategy；研究 task.json 声明的商品页和素材，生成 material.json 后紧接着生成 claims、sources、topic_candidates、selected_topic_id 与 evidence.json，不增加第二次模型调用。"
policy:
  allow_implicit_invocation: false
